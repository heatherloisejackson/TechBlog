<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tech Blog</title>
  {{!-- Foundation --}}
  <link rel="stylesheet" href="https://dhbhdrzi4tiry.cloudfront.net/cdn/sites/foundation.min.css">
  {{!-- Bootstrap --}}
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
  {{!-- FontAwesome --}}
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
    integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous" />
  {{!-- My CSS --}}
  <link rel="stylesheet" href="../../public/css/style.css">
</head>

<body>
  <nav class="navbar sticky-top navbar-dark bg-dark">
    <div class="container-fluid">
      <a href="/" class="navbar-brand">Tech Blog</a>
      <form class="d-flex">
        <div class="input-group input-group-rounded input-custom-size">
          <input class="input-group-field" type="search" placeholder="Search for a post...">
        </div>
      </form>
      <form class="justify-content-end">
        {{#if logged_in}}
        <a href="/dashboard">Hi there, {{ user.username }}</a>
        <button class="btn" id="logout">logout</button>
        {{else}}
        <button class="btn btn-outline-success me-2" type="button"><a href="/login">Log In</a></button>
        {{/if}}
      </form>
    </div>
  </nav>

  <!-- Render the sub layout -->
  {{{ body }}}

  <footer>
    {{!-- need footer --}}
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>